[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dynamic-rag-esg"
version = "0.1.0"

[tool.setuptools.packages.find]
namespaces = true
exclude = [".venv*"]
